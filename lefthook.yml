pre-commit:
    parallel: true
    jobs:
        - name: check
          run: npm run check
        - name: lint
          run: npm run lint
        - name: unit tests
          run: npm run test:unit -- --run
        - name: rust tests
          glob: "src-tauri/**"
          run: npm run test:rust
